# שיעור 3: הפרויקט הראשון שלכם

**משך:** 40 דקות
**מטרה:** לבנות משהו שימושי מאפס ולהרגיש את הכוח של Claude Code

---

## 🎯 מטרות השיעור

בסוף השיעור:
- תבנו כלי שימושי לבד
- תבינו את תהליך העבודה עם Claude Code
- תהיו מוכנים להמשיך לבד

---

## 🛠️ הפרויקט: מארגן קבצים חכם

### מה נבנה?
סקריפט שמארגן תיקייה מבולגנת:
- מזהה סוגי קבצים (תמונות, מסמכים, וידאו...)
- יוצר תיקיות מסודרות
- מעביר כל קובץ למקום הנכון
- מייצר דוח סיכום

### למה זה שימושי?
- כל אחד מכיר את תיקיית Downloads המבולגנת
- חוסך שעות של עבודה ידנית
- אפשר להריץ שוב ושוב

---

## 📋 שלב 1: הכנה (5 דקות)

### יצירת תיקיית ניסוי
```bash
# צרו תיקייה לניסוי
mkdir test-organize
cd test-organize

# הפעילו Claude
claude
```

### בקשו מ-Claude ליצור קבצי דוגמה
```
צור לי 15 קבצים לדוגמה בתיקייה הנוכחית:
- 5 תמונות (jpg, png)
- 4 מסמכים (pdf, docx, txt)
- 3 קבצי וידאו (mp4, mov)
- 3 קבצי מוזיקה (mp3)

תשתמש ב-touch ליצירת קבצים ריקים
```

**Claude יריץ משהו כזה:**
```bash
touch photo1.jpg photo2.jpg photo3.png image4.jpg screenshot5.png
touch document1.pdf report2.docx notes3.txt contract4.pdf
touch video1.mp4 video2.mov clip3.mp4
touch song1.mp3 music2.mp3 podcast3.mp3
```

---

## 🔨 שלב 2: בניית הסקריפט (15 דקות)

### הבקשה ל-Claude
```
צור סקריפט Python בשם organize.py שעושה את הדברים הבאים:

1. סורק את התיקייה הנוכחית
2. מזהה סוגי קבצים לפי סיומת:
   - תמונות: jpg, jpeg, png, gif, bmp
   - מסמכים: pdf, doc, docx, txt, xlsx
   - וידאו: mp4, mov, avi, mkv
   - מוזיקה: mp3, wav, flac
   - אחר: כל השאר
3. יוצר תיקיות לכל קטגוריה (אם צריך)
4. מעביר כל קובץ לתיקייה המתאימה
5. מדפיס סיכום: כמה קבצים הועברו לכל תיקייה

תוסיף הערות בעברית בקוד
```

### מה Claude יכתוב (בערך):
```python
#!/usr/bin/env python3
"""
מארגן קבצים אוטומטי
מעביר קבצים לתיקיות לפי סוג
"""

import os
import shutil
from collections import defaultdict

# הגדרת קטגוריות וסיומות
CATEGORIES = {
    'תמונות': ['.jpg', '.jpeg', '.png', '.gif', '.bmp'],
    'מסמכים': ['.pdf', '.doc', '.docx', '.txt', '.xlsx'],
    'וידאו': ['.mp4', '.mov', '.avi', '.mkv'],
    'מוזיקה': ['.mp3', '.wav', '.flac'],
}

def get_category(filename):
    """מחזיר את הקטגוריה של קובץ לפי הסיומת"""
    ext = os.path.splitext(filename)[1].lower()
    for category, extensions in CATEGORIES.items():
        if ext in extensions:
            return category
    return 'אחר'

def organize_files():
    """מארגן את כל הקבצים בתיקייה הנוכחית"""
    # ספירה לסיכום
    summary = defaultdict(int)

    # עובר על כל הקבצים
    for filename in os.listdir('.'):
        # מדלג על תיקיות ועל הסקריפט עצמו
        if os.path.isdir(filename) or filename == 'organize.py':
            continue

        # מזהה קטגוריה
        category = get_category(filename)

        # יוצר תיקייה אם לא קיימת
        if not os.path.exists(category):
            os.makedirs(category)
            print(f"נוצרה תיקייה: {category}")

        # מעביר את הקובץ
        shutil.move(filename, os.path.join(category, filename))
        summary[category] += 1

    # מדפיס סיכום
    print("\n📊 סיכום:")
    print("-" * 30)
    for category, count in summary.items():
        print(f"  {category}: {count} קבצים")
    print(f"\n✅ סה\"כ: {sum(summary.values())} קבצים אורגנו!")

if __name__ == '__main__':
    organize_files()
```

---

## ▶️ שלב 3: הרצה ובדיקה (10 דקות)

### בקשו מ-Claude להריץ
```
תריץ את הסקריפט ותראה לי מה קרה
```

**Claude יריץ:**
```bash
python organize.py
```

**פלט צפוי:**
```
נוצרה תיקייה: תמונות
נוצרה תיקייה: מסמכים
נוצרה תיקייה: וידאו
נוצרה תיקייה: מוזיקה

📊 סיכום:
------------------------------
  תמונות: 5 קבצים
  מסמכים: 4 קבצים
  וידאו: 3 קבצים
  מוזיקה: 3 קבצים

✅ סה"כ: 15 קבצים אורגנו!
```

### אימות
```
תראה לי את מבנה התיקיות עכשיו
```

---

## 🔧 שלב 4: שיפורים (10 דקות)

### שדרוג 1: הוספת לוג
```
תוסיף לסקריפט שמירת לוג לקובץ organize_log.txt
עם תאריך ושעה
```

### שדרוג 2: מצב יבש (Dry Run)
```
תוסיף אפשרות להריץ ב-dry run
שרק מראה מה היה קורה בלי להזיז קבצים
```

### שדרוג 3: גיבוי
```
תוסיף אפשרות לגבות קבצים לפני העברה
```

---

## 🎁 בונוס: הפיכה לפקודה

### שאלו את Claude:
```
איך אני יכול להריץ את הסקריפט הזה מכל מקום
בלי להקליד python organize.py?
```

**Claude יסביר:**
- הוספת shebang
- הפיכה ל-executable
- הוספה ל-PATH

---

## ✅ תרגיל עצמאי

### משימה: התאמה אישית
בחרו אחד:

**אפשרות A: קטגוריות מותאמות**
```
שנה את הסקריפט שיתאים לעבודה שלי:
- קבצי עבודה: xlsx, pptx, pdf
- טיוטות: _draft, _temp
- לסריקה: scan_, IMG_
```

**אפשרות B: ארגון לפי תאריך**
```
צור גרסה שמארגנת לפי תאריך יצירה:
- 2024/01/
- 2024/02/
- וכו'
```

**אפשרות C: ניקוי כפולים**
```
הוסף זיהוי של קבצים כפולים
והעברתם לתיקייה נפרדת
```

---

## 📝 מה למדנו?

### תהליך העבודה
1. **תיאור הבעיה** - מה רוצים לפתור
2. **בקשה ברורה** - מה הסקריפט צריך לעשות
3. **הרצה ובדיקה** - לוודא שעובד
4. **שיפור איטרטיבי** - להוסיף פיצ'רים

### עקרונות חשובים
- ✅ להתחיל קטן ולגדול
- ✅ לבדוק אחרי כל שינוי
- ✅ לשמור גרסאות (git)
- ✅ לתעד מה עשינו

---

## 🎓 סיכום הקורס - מבוא

### מה כיסינו:
1. **שיעור 1:** מה זה Claude Code והתקנה
2. **שיעור 2:** פקודות בסיסיות וניווט
3. **שיעור 3:** בניית פרויקט אמיתי

### מה הלאה?
- **תרגול יומי:** השתמשו ב-Claude Code לדברים קטנים
- **קורס מתקדם:** Skills, Subagents, אוטומציה
- **קהילה:** הצטרפו לקבוצת WhatsApp

---

## 📚 חומרים להמשך

- [דף עזר (Cheatsheet)](../claude-code-cheatsheet.pdf)
- [מדריכי Skills](../../skills-pdfs/)
- [דוגמאות קוד](../../examples/)

---

## 🏆 תעודת סיום

סיימתם את קורס המבוא ל-Claude Code!

**עכשיו אתם יכולים:**
- ✅ להתקין ולהפעיל Claude Code
- ✅ לנווט ולעבוד עם קבצים
- ✅ לבנות כלים שימושיים
- ✅ להמשיך ללמוד לבד

**הצעד הבא:** קורס מתקדם - Skills & Automation

---

> "הדרך הכי טובה ללמוד היא לעשות.
> עכשיו לכו ותבנו משהו!"
